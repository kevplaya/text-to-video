# Core AI/ML Libraries
google-genai>=1.0.0
requests>=2.28.0  # For Hugging Face Inference API

# Image Generation - FLUX.1-dev (local)
# Note: torch, diffusers, and sentencepiece are optional, only needed for --image-model flux
# Install with: pip install torch diffusers sentencepiece
# torch>=2.0.0  # Uncomment if using FLUX.1-dev locally
# diffusers>=0.30.0  # Uncomment if using FLUX.1-dev locally
# sentencepiece>=0.1.99  # Uncomment if using FLUX.1-dev locally

# Video and Audio Processing
moviepy>=1.0.3
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# TTS
gTTS>=2.4.0

# Optional - ElevenLabs TTS
elevenlabs>=0.2.0
